apiVersion: v1
kind: Service
metadata:
  name: {{ include "theai.fullname" . }}-backend
  labels:
    {{- include "theai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "theai.fullname" . }}-backend